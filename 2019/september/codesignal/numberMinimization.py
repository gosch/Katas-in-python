import itertools

def find_minimization(n, d):
    minimum = d
    str_value = str(n).split()

    return minimum


def numberMinimization(n, d):
    minimum = find_minimization(n, d)
    return minimum

print(numberMinimization(501, 3))

